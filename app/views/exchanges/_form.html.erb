<%= form_for(@exchange) do |f| %>
  <% if @exchange.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exchange.errors.count, "error") %> prohibited this exchange from being saved:</h2>

      <ul>
      <% @exchange.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.select :description, options_for_select([["deposit", "deposit"], ["direct deposit", "direct deposit"], [" atm withdrawal", "atm withdrawal"], ["withdrawal", "withdrawal"], ["check", "check"], ["debit card", "debit card"]])%>
  </div>
  <div class="field">
    <%= f.label :payee %><br>
    <%= f.text_field :payee %>
  </div>
  <div class="field">
    <%= f.label :credits %><br>
    <%= f.text_field :credits %>
  </div>
  <div class="field">
    <%= f.label :debits %><br>
    <%= f.text_field :debits %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
